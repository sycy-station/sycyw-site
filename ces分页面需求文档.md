# CES 官网分页面补充需求文档

> 项目目录：`C:\Users\dongy\Desktop\ces`  
> 内容结构参考：森韵次元原官网 <https://www.syciyuan.cn/>  
> 文档版本：V1.0  
> 编写日期：2026-09-01

## 1. 项目背景

`ces` 当前是一个无框架、多页面静态站点。首页、顶部索引、菜单、主题切换和跨文档转场已经完成，页面由 `build/pages.js` 与 `build/build.js` 统一生成。

当前项目存在以下问题：

- 站点导航仍是“系统设计 / 交互研究 / 品牌语言 / 关于我们 / 动效编排 / 实验归档”，与森韵次元原官网的真实业务信息架构不一致。
- `system-design.html`、`interaction.html`、`brand.html`、`motion.html`、`archive.html` 的正文仍是 `CONTENT / PENDING` 占位内容。
- “关于我们”页已有定制内容和样式，但字段、事实与原官网不完全一致，需要按本需求重新校准。
- 当前页脚仍含 `hello@ces.studio`、`href="#"` 等占位信息。

## 2. 改造目标

在保留 `ces` 现有黑白视觉体系、动效语言和构建方式的前提下，按森韵次元原官网的栏目与字段补全分页面。

本次采用原官网的真实一级信息架构：

1. 首页(主页面)
2. 关于我们
3. 产品服务
4. 团队介绍
5. 加入我们
6. 森韵次元（无需分页面）
7. 曲词识库（无需分页面）

首页仍为入口页；需要建设 6个分页面。现有“系统设计、交互研究、品牌语言、动效编排、实验归档”不再作为正式导航栏目。

## 3. 基本原则

- **字段结构以原官网为准**：页面模块、列表字段和内容层级参照 `syciyuan.cn`。
- **视觉表现以 `ces` 为准**：不复制原官网的粉色卡片样式，继续使用当前黑白、刻度、频率和转场体系。
- **原官网文案不是无条件可信数据**：旧站中的占位链接、模拟招聘、过期时间和错误公司名必须先校验。
- **构建源码优先**：修改 `build/` 下的数据与模板，再运行构建；根目录 HTML 是生成产物，不作为唯一修改源。
- **移动端、无障碍和减弱动效不可降级**：新增内容必须延续现有 `prefers-reduced-motion`、`noscript` 与响应式策略。

## 4. 页面与文件映射

| 编号 | 页面 | slug / 输出文件 | 英文副标题 | 页面一句话描述 |
| --- | --- | --- | --- | --- |
| 01 | 关于我们 | `about` / `about.html` | `ABOUT US` | 用创意和科技连接二次元与现实 |
| 02 | 产品服务 | `products` / `products.html` | `PRODUCTS & SERVICES` | 融合二次元文化与科技创新的产品与合作项目 |
| 03 | 团队介绍 | `team` / `team.html` | `OUR TEAM` | 由二次元爱好者与技术成员组成的团队 |
| 04 | 加入我们 | `join` / `join.html` | `JOIN US` | 与热爱二次元文化和科技创新的人一起创造未来 |

### 4.1 现有页面处理

- 保留并重构 `about.html`。
- 新增 `products.html`、`team.html`、`join.html`。
- 从 `PAGES`、首页轨道、顶部索引、全屏菜单和 sitemap 中移除以下旧栏目：
  - `system-design`
  - `interaction`
  - `brand`
  - `motion`
  - `archive`
- 旧生成文件在新导航和链接全部通过后再删除，避免迁移过程中出现断链。

### 4.2 首页配套调整

- 首页轨道由 6 项改为 4 项。
- `stage.css` 中现有 `pos-1` 至 `pos-6` 是手工坐标，必须重新设计 4 项布局，不能只删除两项数据。
- `SPLASH_SIDES` 改为现存 slug，例如 `products` 与 `team`。
- 首页、菜单、分页面顶部索引中的栏目顺序必须完全一致。
- `freq` 保持严格递增，可暂定为 `88.1 / 94.7 / 101.9 / 104.6`；它是当前视觉系统字段，不是原官网业务数据。

## 5. 全站公共字段

### 5.1 页面元数据

每个页面必须具备以下字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `slug` | string | 是 | 文件名与页面唯一标识 |
| `no` | string | 是 | 两位页面编号 |
| `title` | string | 是 | 中文页面名称 |
| `sub` | string | 是 | 英文副标题 |
| `desc` | string | 是 | 页面摘要，同时作为默认 SEO description |
| `year` | string | 是 | 当前首页视觉信息字段，建议统一为 `2024` |
| `freq` | string | 是 | 顶部刻度读数，必须单调递增 |
| `seoTitle` | string | 是 | 格式：`页面名称 — 森韵次元` |
| `seoDescription` | string | 是 | 80 至 120 个中文字符以内 |
| `canonicalPath` | string | 是 | 页面正式路径 |

### 5.2 页头和导航

- 品牌名：森韵次元。
- Logo 点击返回 `index.html`。
- 导航项：关于我们、产品服务、团队介绍、加入我们。
- 当前页面使用 `aria-current="page"`。
- 桌面端和移动端菜单使用同一份 `PAGES` 数据生成，禁止手写两套栏目。

### 5.3 页脚

页脚字段按原官网保留以下结构：

| 模块 | 字段 |
| --- | --- |
| 品牌信息 | Logo、品牌名、品牌简介 |
| 快速链接 | 首页、关于我们、产品服务、团队介绍、加入我们 |
| 联系方式 | 电话、邮箱、办公地址 |
| 社交渠道 | 哔哩哔哩、微博、QQ；每项包含名称、URL、图标或文字标识 |
| 法务信息 | 公司全称、版权年份、备案号、隐私政策、服务条款 |

原官网当前公开的电话、邮箱和地址只能作为待确认来源值。上线前必须由负责人确认，不得直接把旧站值视为长期有效配置。

## 6. 关于我们页

### 6.1 页面目标

说明公司是谁、为何成立、坚持什么，以及从前身项目到森韵次元的发展过程。

### 6.2 模块与字段

#### A. 首屏 Hero

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `heroTitle` | 是 | 原官网语义：用创意和科技连接二次元与现实 |
| `heroDescription` | 是 | 公司成立时间、业务方向与价值主张 |
| `heroImage` | 是 | 主视觉图片路径 |
| `heroImageAlt` | 是 | 可理解的图片替代文本 |

#### B. 我们的故事

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `sectionTitle` | 是 | 固定为“我们的故事” |
| `storyImage` | 否 | 公司或项目相关真实图片 |
| `storyParagraphs[]` | 是 | 至少 2 段，说明起点、转折和当前状态 |

#### C. 我们的价值观

字段：`icon`、`title`、`description`、`order`。

首期保留原官网的 3 项结构：

- 创新无界
- 用户至上
- 热爱与激情

#### D. 我们的使命

字段：`title`、`description`、可选强调语 `statement`。

#### E. 发展历程

每条字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `year` | 是 | 年份或季度 |
| `title` | 是 | 里程碑名称 |
| `description` | 是 | 事件说明 |
| `status` | 否 | `completed / current / planned` |
| `order` | 是 | 排序值 |

原官网包含 2018、2022、2024、2024 年末、2025、2025 年末等节点。涉及“预计上线”的旧时间不得原样保留，必须按当前实际进度更新。

## 7. 产品服务页

### 7.1 页面目标

展示公司自有项目、合作服务和友链项目，并让用户可以查看项目特点、参数和外部详情。

### 7.2 模块与字段

#### A. 首屏 Hero

字段：`heroTitle`、`heroDescription`、`heroImage`、`heroImageAlt`。

#### B. 产品分类

每项字段：`id`、`label`、`order`。

原官网分类：

- 全部产品
- 托管支持
- 开发项目
- 友链推送
- 初创论坛

交互要求：点击分类后仅展示对应产品；无 JavaScript 时默认展示全部产品。

#### C. 产品详情

每个产品字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `id` | 是 | 产品唯一标识 |
| `categoryId` | 是 | 对应分类 |
| `categoryLabel` | 是 | 页面显示标签 |
| `name` | 是 | 产品或合作项目名称 |
| `description` | 是 | 产品完整介绍 |
| `image` | 是 | 产品图或真实页面截图 |
| `imageAlt` | 是 | 图片替代文本 |
| `features[]` | 是 | 3 项为宜；每项含 `title`、`description`、可选 `icon` |
| `primaryAction` | 是 | `label`、`url`、`external` |
| `secondaryAction` | 否 | 仅在确有可用功能时显示 |
| `status` | 否 | 运营中、开发中、已归档等 |

原官网首期产品数据范围：

| 分类 | 名称 | 备注 |
| --- | --- | --- |
| 托管支持 | 林枫云 | 外链域名在首页与产品页存在不一致，上线前确认 |
| 开发项目 | 森韵次元坞 | 原官网出现多个项目域名，必须确认正式入口 |
| 友链推送 | MineBBS | 确认合作关系和展示文案仍然有效 |
| 初创论坛 | 天空府邸论坛 | 首页与产品页入口不一致，上线前确认 |

#### D. 产品规格

规格模块为可选字段，只在确有参数的产品中展示。

- `specGroupTitle`
- `specGroupDescription`
- `specRows[]`
  - `name`
  - `value`
  - `note`（可选）

#### E. 案例或推荐位

字段：`title`、`description`、`image`、`imageAlt`、`url`、`status`。

原官网的“9.9/月广告位”和“未购入”是旧站占位文案，不应迁移到正式页面。没有真实案例时整段隐藏，不展示空卡片。

## 8. 团队介绍页

### 8.1 页面目标

展示领导团队、核心成员、团队文化与工作环境，增强企业可信度和招募吸引力。

### 8.2 模块与字段

#### A. 首屏 Hero

字段：`heroTitle`、`heroDescription`、`heroImage`、`heroImageAlt`。

#### B. 领导团队

每位成员字段：

- `name`
- `role`
- `avatar`
- `avatarAlt`
- `bio`
- `skills[]`（可选）
- `socialLinks[]`（可选）
- `order`

#### C. 核心团队

每位成员字段：`name`、`role`、`avatar`、`avatarAlt`、`order`。若无公开简介，不编造个人履历。

#### D. 团队文化

每项字段：`icon`、`title`、`description`、`order`。

原官网首期结构：

- 创新无界
- 协作共赢
- 持续成长

#### E. 工作环境

字段：`images[]`，每张图片包含 `src`、`alt`、可选 `caption`。只使用真实、已授权的团队或工作环境图片。

#### F. 加入团队 CTA

字段：`title`、`description`、`buttonLabel`、`buttonUrl`，按钮指向 `join.html`。

## 9. 加入我们页

### 9.1 页面目标

介绍团队文化和福利，展示真实开放职位与招聘流程，并提供明确的申请入口。

### 9.2 模块与字段

#### A. 首屏 Hero

字段：`heroTitle`、`heroDescription`、`heroImage`、`heroImageAlt`。

#### B. 我们的文化

每项字段：`icon`、`title`、`description`、`order`。

原官网结构包含：创新无界、用户至上、热爱与激情、团队协作、持续成长、工作与生活平衡。

#### C. 员工福利

每项字段：`icon`、`title`、`description`、`order`。

原官网结构包含：弹性工作制、健康保障、学习发展、团队活动、股权激励。以上福利属于需要确认的事实信息，未落实的福利不得展示。

#### D. 职位筛选

每项字段：`id`、`label`、`order`。筛选项只能来自当前职位数据，避免出现没有职位的空分类。

#### E. 职位卡

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `id` | 是 | 职位唯一标识 |
| `categoryId` | 是 | 对应筛选分类 |
| `categoryLabel` | 是 | 职位类别 |
| `title` | 是 | 职位名称 |
| `location` | 是 | 工作地点或“远程” |
| `description` | 是 | 职位说明 |
| `requirements[]` | 是 | 任职要求列表 |
| `applyLabel` | 是 | 例如“申请职位” |
| `applyUrl` | 是 | 可用邮箱、表单或招聘链接，禁止 `#` |
| `status` | 是 | `open / closed` |

原官网明确注明现有招聘为非真实信息，因此 UI/UX 设计师、内容营销专员、产品运营经理不能直接作为正式招聘内容迁移。没有已确认职位时显示“暂无开放职位”，但保留职位字段结构。

#### F. 招聘流程

每一步字段：`step`、`title`、`description`、`order`。

原官网流程结构：简历投递、初步面试、技术或专业面试、终面、Offer 发放。页面不得写死具体负责人姓名和响应时限，除非招聘负责人确认。

#### G. 页面 CTA

字段：`title`、`description`、`primaryAction`、可选 `secondaryAction`。所有按钮必须有真实目标。

## 10. 内容校验清单

以下旧站内容不得直接复制，必须先确认或改正：

| 问题 | 处理要求 |
| --- | --- |
| 哔哩哔哩、微博链接中存在 `1234567890` 占位 ID | 替换为真实官方账号；无账号则隐藏 |
| 多个按钮使用 `href="#"` | 改为真实链接或移除按钮 |
| 招聘页明确写有“非真实信息” | 未确认职位时展示空状态 |
| 团队介绍文案出现“梦幻科技” | 统一核对为正确公司或项目名称 |
| 产品入口域名互相冲突 | 逐项确认唯一正式 URL |
| 2025 年末上线、预计 5 月内测等时间已过期 | 按当前进度更新或删除预测 |
| 团队职务、人数、履历可能已变化 | 由负责人逐项确认 |
| 联系方式和办公地址可能已变化 | 上线前进行最终确认 |
| 产品参数、合作关系、广告价格可能失效 | 仅展示有凭据且仍有效的信息 |

## 11. 交互要求

- 首页进入分页面、分页面之间切换、返回首页继续使用现有 `ces-nav` 转场信号。
- 顶部索引在窄屏可横向滚动，当前页自动处于可见区域。
- 产品分类和职位筛选支持键盘操作，并提供清楚的选中状态。
- 外部链接使用 `target="_blank"` 与 `rel="noopener noreferrer"`。
- 所有按钮必须可操作；不存在功能时不显示按钮，不使用假链接。
- 所有首屏和滚动揭示动画尊重 `prefers-reduced-motion`。
- JavaScript 不可用时，正文、产品和职位仍可完整阅读。

## 12. 技术实现要求

### 12.1 数据与模板

- `build/pages.js` 继续作为导航和页面元数据唯一来源。
- 在 `build/partials/` 下建立：
  - `body-about.html`
  - `body-products.html`
  - `body-team.html`
  - `body-join.html`
- 重复列表内容应集中为结构化数据，再由构建脚本生成；至少不要在首页、菜单和分页面之间重复维护栏目名称。
- `build/build.js` 已具备按 `body-<slug>.html` 自动选择正文模板的逻辑，应沿用该机制。
- 页面特有样式使用独立文件，并由构建脚本按 slug 加载，例如 `products.css`、`team.css`、`join.css`。
- 当前工作区已有未提交的 `about.html`、`build/build.js`、`body-about.html`、`page.html`、`about.css` 和设计测试改动，实施时必须在这些改动基础上继续，禁止覆盖或回退。

### 12.2 生成与清理

- 修改源码后运行 `node build/build.js` 生成根目录 HTML。
- 构建输出应为 `index.html` 加 4 个正式分页面。
- 确认导航、站点地图和所有内部链接通过后，再清理旧栏目生成文件。
- 正式域名确定后，在 `SITE_URL` 填写 `https://www.syciyuan.cn`，重新生成 canonical、Open Graph 和 sitemap。

## 13. 响应式与无障碍要求

- 基准视口：375px、768px、1440px。
- 正文不得与顶部索引、页头或页脚重叠。
- 除顶部索引等明确可滚动组件外，不允许页面整体横向溢出。
- 页面保持唯一 `h1`，各模块按 `h2`、卡片标题按 `h3` 递进。
- 图片均提供准确 `alt`；纯装饰图片使用空 `alt`。
- 筛选控件必须使用可聚焦按钮，并同步 `aria-pressed` 或等效状态。
- 当前导航项使用 `aria-current="page"`。
- 文本对比度达到 WCAG AA 基础要求。

## 14. SEO 要求

- 每页提供唯一 `title` 和 `description`。
- canonical 指向 `https://www.syciyuan.cn/<page>.html`。
- Open Graph 字段包含 `og:title`、`og:description`、`og:url`、`og:image`。
- sitemap 只包含首页和 4 个正式分页面。
- 删除旧页面后，如线上已有旧 URL，需要配置 301 重定向或保留兼容跳转页。

## 15. 验收标准

### 15.1 内容验收

- 4 个分页面全部生成并可从首页进入。
- 页面模块和字段与本需求第 6 至 9 节一致。
- 全站不存在 `CONTENT / PENDING`、`这一页的内容待补`、`href="#"` 或未购入广告占位。
- 不再出现“系统设计 / 交互研究 / 品牌语言 / 动效编排 / 实验归档”等旧导航栏目。
- 所有公司事实、团队信息、联系方式、产品入口和招聘内容均完成负责人确认。

### 15.2 功能验收

- 首页、分页面、菜单、顶部索引的链接和顺序一致。
- 产品筛选与职位筛选工作正常；刷新页面后默认状态合理。
- 所有外链可访问，内部链接无 404。
- 深色、浅色主题均可阅读，切换时无明显闪白。
- 首页进入、分页面互跳、返回首页的转场连续，无首帧跳变。
- 禁用 JavaScript 后正文仍可见。

### 15.3 视觉与设备验收

- 在 375px、768px、1440px 宽度下无文本截断、模块重叠和异常横向滚动。
- 卡片、时间轴、产品规格和团队成员列表在长文本下不破版。
- 减弱动效模式下无持续动画，导航和内容仍可正常使用。

### 15.4 构建验收

- `node build/build.js` 无报错。
- 根目录生成首页与 4 个正式分页面。
- sitemap、robots、canonical 与 Open Graph 使用正确正式域名。
- `git diff` 不包含无关文件、用户现有改动回退或大面积行尾噪音。

## 16. 实施优先级

### P0：信息架构与数据

- 调整 `PAGES` 为 4 个正式分页面。
- 确认产品、团队、联系方式、招聘和域名数据。
- 重做首页 4 项轨道布局。

### P1：页面建设

- 完成关于我们、产品服务、团队介绍、加入我们正文模板。
- 完成页面特有样式、筛选交互和响应式适配。
- 更新页脚、SEO 与 sitemap。

### P2：质量验证

- 全链路链接检查。
- 桌面与移动端视觉检查。
- 主题、减弱动效、无 JavaScript 和跨页转场检查。
- 清理旧页面与占位内容。

## 17. 本次范围之外

- 不建设后台 CMS 或管理系统。
- 不实现真实消息发送、简历上传或用户账号系统；如需这些能力，应另立后端需求。
- 不复制原官网视觉风格。
- 不擅自发布、提交招聘或修改线上官网。

